# Рекомендации по оптимизации производительности

## 🔍 Анализ проблем

### 1. **AnimatedGradientBackground - Canvas анимация**
- Canvas анимация работает постоянно на каждой странице
- Использует requestAnimationFrame без оптимизации
- Перерисовка на каждый кадр (60 FPS)

**Рекомендации:**
- Добавить `will-change: transform` для GPU-ускорения
- Оптимизировать количество геометрических фигур
- Использовать CSS-анимации вместо Canvas где возможно
- Добавить паузу анимации при неактивной вкладке (Page Visibility API)

### 2. **Большие компоненты без мемоизации**
- BuyTokensSection, DashboardSection, InvestmentOptionsWithContract - большие компоненты
- Нет React.memo для предотвращения лишних ререндеров
- Нет useMemo/useCallback для тяжелых вычислений

**Рекомендации:**
- Обернуть компоненты в React.memo
- Использовать useMemo для фильтрации списков
- Использовать useCallback для обработчиков событий

### 3. **CoinGecko API запросы**
- Запросы на каждой загрузке компонента
- Нет дебаунсинга
- Может вызывать rate limiting

**Рекомендации:**
- Увеличить кэш до 10-15 минут
- Использовать SWR или React Query для кэширования
- Добавить фоновое обновление

### 4. **Framer Motion - тяжелая библиотека**
- Большой размер бандла (~50-70KB)
- Используется только в LoginModal

**Рекомендации:**
- Заменить на CSS-анимации или более легкую библиотеку
- Или использовать tree-shaking для импорта только нужных компонентов

### 5. **Ethers.js - большая библиотека**
- ~200KB+ в бандле
- Можно оптимизировать импорты

**Рекомендации:**
- Использовать tree-shaking
- Импортировать только нужные модули

### 6. **Отсутствие code splitting**
- Все компоненты загружаются сразу
- Нет динамических импортов для тяжелых компонентов

**Рекомендации:**
- Использовать dynamic imports для Dashboard, ProfileTab и других тяжелых компонентов
- Lazy loading для модальных окон

### 7. **Неоптимизированные изображения**
- Нет next/image для оптимизации изображений (если есть)

### 8. **Множественные useEffect без зависимостей**
- Могут вызывать лишние ререндеры

## 📋 План оптимизации (приоритет)

### Высокий приоритет ⚡

1. **Оптимизация AnimatedGradientBackground**
   - Добавить паузу при неактивной вкладке
   - Уменьшить частоту обновления
   - Использовать CSS transforms вместо перерисовки всего canvas

2. **Мемоизация компонентов**
   - React.memo для всех больших компонентов
   - useMemo для фильтрованных списков
   - useCallback для обработчиков

3. **Code Splitting**
   - Dynamic imports для Dashboard и его табов
   - Lazy loading для модальных окон

### Средний приоритет 📊

4. **Оптимизация API запросов**
   - Увеличить кэш CoinGecko
   - Использовать React Query или SWR

5. **Оптимизация бандла**
   - Tree-shaking для ethers.js
   - Оптимизация framer-motion импортов

### Низкий приоритет 🔧

6. **CSS оптимизации**
   - Использовать CSS-анимации вместо JS где возможно
   - Оптимизация Tailwind CSS (purge unused)

7. **Дополнительные оптимизации**
   - Service Worker для кэширования
   - Prefetching для важных страниц

## 🚀 Быстрые победы (можно сделать сразу)

1. Добавить `will-change` для canvas
2. Включить паузу анимации при неактивной вкладке
3. Обернуть крупные компоненты в React.memo
4. Добавить dynamic imports для Dashboard табов
5. Увеличить кэш CoinGecko до 10 минут

