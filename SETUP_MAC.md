# Настройка проекта на macOS

Инструкция для продолжения разработки El Toro Negro на MacBook.

## 📋 Предварительные требования

### 1. Установка Node.js и npm

Если Node.js еще не установлен:

```bash
# Установка через Homebrew (рекомендуется)
brew install node

# Или скачайте с официального сайта: https://nodejs.org/
# Рекомендуемая версия: LTS (v18 или выше)

# Проверка версии
node --version
npm --version
```

### 2. Установка Git

```bash
# Проверка наличия Git
git --version

# Если не установлен, установите через Homebrew
brew install git
```

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
# Перейдите в папку, где хотите разместить проект
cd ~/Desktop  # или любая другая папка

# Клонируйте репозиторий
git clone https://github.com/alekseevpo/ElToroNegro.git

# Перейдите в папку проекта
cd ElToroNegro
```

### 2. Установка зависимостей для смарт-контрактов

```bash
# Установка зависимостей корневого проекта (Hardhat и др.)
npm install
```

### 3. Установка зависимостей для фронтенда

```bash
# Перейдите в папку frontend
cd frontend

# Установите зависимости
npm install

# Вернитесь в корень проекта
cd ..
```

## ⚙️ Настройка окружения

### Переменные окружения

Создайте файлы `.env` для локальной разработки:

#### Для Hardhat (корень проекта)

Создайте `.env` в корне проекта (скопируйте пример ниже и заполните своими значениями):

```env
# Сеть (например, Sepolia testnet или localhost)
NETWORK=localhost

# Приватный ключ для деплоя (НИКОГДА не коммитьте!)
PRIVATE_KEY=your_private_key_here

# RPC URL (для тестовой сети)
RPC_URL=https://sepolia.infura.io/v3/your_infura_key

# Chainlink VRF Subscription ID (если используете)
VRF_SUBSCRIPTION_ID=your_subscription_id

# Chainlink VRF Key Hash
VRF_KEY_HASH=your_key_hash
```

**Создание файла:**

```bash
# В корне проекта
touch .env
# Затем откройте файл в редакторе и вставьте переменные выше
nano .env  # или open .env (для открытия в редакторе по умолчанию)
```

#### Для Frontend (папка frontend)

Создайте `.env.local` в папке `frontend/`:

```env
# Stripe (для платежей)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_key
STRIPE_SECRET_KEY=sk_test_your_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Адреса смарт-контрактов (после деплоя)
NEXT_PUBLIC_INVESTMENT_POOL_ADDRESS=0x...
NEXT_PUBLIC_LOTTERY_ADDRESS=0x...
NEXT_PUBLIC_BITCOIN_BET_ADDRESS=0x...

# Network Chain ID
NEXT_PUBLIC_CHAIN_ID=11155111  # Sepolia testnet
# или
# NEXT_PUBLIC_CHAIN_ID=1  # Ethereum mainnet
```

**Создание файла:**

```bash
# В папке frontend
cd frontend
touch .env.local
# Затем откройте файл и вставьте переменные выше
nano .env.local
```

## 🏃 Запуск проекта

### Локальная Hardhat сеть (для тестирования контрактов)

```bash
# В корне проекта
npx hardhat node
```

Это запустит локальную блокчейн сеть на `http://127.0.0.1:8545`

### Компиляция контрактов

```bash
npx hardhat compile
```

### Запуск тестов

```bash
npx hardhat test
```

### Запуск фронтенда

```bash
# Перейдите в папку frontend
cd frontend

# Запустите dev сервер
npm run dev
```

Фронтенд будет доступен на `http://localhost:3000`

## 📝 Полезные команды

### Git команды

```bash
# Проверка статуса
git status

# Получение последних изменений
git pull origin main

# Создание новой ветки для разработки
git checkout -b feature/your-feature-name

# Коммит изменений
git add .
git commit -m "Описание изменений"
git push origin feature/your-feature-name
```

### Hardhat команды

```bash
# Компиляция контрактов
npx hardhat compile

# Запуск тестов
npx hardhat test

# Запуск тестов с покрытием
npx hardhat coverage

# Деплой контракта
npx hardhat run scripts/deploy.js --network sepolia
```

### Next.js команды (в папке frontend)

```bash
# Разработка
npm run dev

# Сборка для продакшена
npm run build

# Запуск продакшен версии
npm start

# Линтинг
npm run lint
```

## 🔧 Устранение проблем

### Проблемы с правами доступа

Если возникают проблемы с правами доступа:

```bash
# Для npm пакетов
sudo chown -R $(whoami) ~/.npm

# Для node_modules
sudo chown -R $(whoami) node_modules
```

### Очистка кэша

```bash
# Очистка npm кэша
npm cache clean --force

# Удаление node_modules и переустановка
rm -rf node_modules package-lock.json
npm install
```

### Проблемы с портами

Если порт 3000 занят:

```bash
# Найти процесс, использующий порт
lsof -ti:3000

# Убить процесс
kill -9 $(lsof -ti:3000)
```

Или измените порт в `package.json` фронтенда:

```json
{
  "scripts": {
    "dev": "next dev -p 3001"
  }
}
```

## 📦 Структура проекта

```
ElToroNegro/
├── contracts/          # Смарт-контракты Solidity
├── frontend/           # Next.js приложение
│   ├── app/           # Страницы и API routes
│   ├── components/    # React компоненты
│   ├── lib/           # Утилиты
│   └── ...
├── scripts/           # Скрипты деплоя
├── tests/             # Тесты контрактов
├── docs/              # Документация
└── hardhat.config.js  # Конфигурация Hardhat
```

## 🔐 Безопасность

⚠️ **ВАЖНО:**

1. Никогда не коммитьте файлы `.env` с реальными ключами
2. Используйте `.env.example` для документации
3. Для продакшена используйте переменные окружения на сервере
4. Храните приватные ключи в безопасном месте

## 📚 Дополнительная документация

- [README.md](./README.md) - Основная документация проекта
- [docs/](./docs/) - Подробная документация по компонентам
- [OPTIMIZATION_RECOMMENDATIONS.md](./OPTIMIZATION_RECOMMENDATIONS.md) - Рекомендации по оптимизации

## 💡 Полезные ссылки

- [Hardhat Documentation](https://hardhat.org/docs)
- [Next.js Documentation](https://nextjs.org/docs)
- [Ethers.js Documentation](https://docs.ethers.org/)
- [Chainlink Documentation](https://docs.chain.link/)

## 🆘 Получить помощь

Если возникли проблемы:
1. Проверьте документацию в папке `docs/`
2. Проверьте issues на GitHub
3. Создайте новый issue с описанием проблемы

