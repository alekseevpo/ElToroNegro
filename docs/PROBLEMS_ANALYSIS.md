# Анализ проблем проекта El Toro Negro

**Дата анализа:** 2025-01-02  
**Статус:** ✅ Большинство проблем исправлено  
**См. также:** [FIXES_APPLIED.md](./FIXES_APPLIED.md) - детальный список примененных исправлений

## 🔴 Критические проблемы

### 1. Неполная миграция с localStorage на API

**Проблема:** Frontend все еще использует `localStorage` для хранения профилей, хотя API endpoints уже созданы.

**Детали:**
- ✅ API endpoints созданы (`/api/profile`, `/api/profile/[address]`)
- ✅ Хук `useProfile` использует API
- ❌ Функции в `lib/profile-utils.ts` все еще используют `localStorage`:
  - `getUserProfile()` - читает из localStorage
  - `saveUserProfile()` - пишет в localStorage
  - `initializeProfile()` - создает профиль в localStorage
  - `updateProfile()` - обновляет в localStorage
  - `connectWallet()` - использует localStorage
  - И другие функции...

**Файлы, использующие localStorage:**
- `frontend/lib/profile-utils.ts` - основные функции
- `frontend/components/DashboardSection.tsx` - для KYC banner dismissed
- `frontend/contexts/AuthContext.tsx` - возможно, для временного хранения

**Последствия:**
- Данные не синхронизируются между устройствами
- Потеря данных при очистке браузера
- Невозможность централизованного управления данными
- Проблемы с масштабированием

**Решение:**
1. Заменить все вызовы `getUserProfile()` на `useProfile()` hook
2. Заменить `saveUserProfile()` на `useProfileMutation()` hook
3. Удалить или пометить как deprecated функции в `profile-utils.ts`
4. Обновить все компоненты, использующие старые функции

---

### 2. Отсутствие аутентификации и защиты API

**Проблема:** API endpoints не защищены, любой может читать/изменять профили.

**Детали:**
- ❌ Нет JWT токенов или NextAuth.js
- ❌ Нет проверки владельца адреса
- ❌ Нет rate limiting
- ❌ Нет валидации входных данных (Zod)

**Последствия:**
- Любой может получить доступ к чужим профилям
- Возможность подделки данных
- Отсутствие безопасности

**Решение:**
1. Реализовать JWT токены или NextAuth.js
2. Добавить проверку подписи сообщений для Web3 адресов
3. Добавить валидацию через Zod
4. Добавить rate limiting

---

## 🟡 Важные проблемы

### 3. Неполная интеграция frontend со смарт-контрактами

**Проблема:** Согласно README, полная интеграция frontend со смарт-контрактами еще в разработке.

**Детали:**
- ✅ InvestmentPool - частично интегрирован
- ⏭️ Lottery - не полностью интегрирован
- ⏭️ BitcoinPriceBet - не полностью интегрирован

**Решение:**
- Завершить интеграцию всех контрактов
- Добавить обработку ошибок
- Добавить loading states

---

### 4. Отсутствие тестов для API

**Проблема:** Нет unit-тестов для API routes.

**Детали:**
- ✅ Есть тесты для лотереи (`lib/__tests__/lottery-utils.test.ts`)
- ✅ Есть API-тесты для lottery endpoints (`__tests__/api/lottery.test.ts`)
- ❌ Нет тестов для profile API
- ❌ Нет тестов для stats API
- ❌ Нет тестов для auth API

**Решение:**
- Добавить unit-тесты для всех API endpoints
- Добавить integration тесты для БД операций

---

### 5. Отсутствие миграции данных из localStorage

**Проблема:** Существующие пользователи с данными в localStorage не смогут получить доступ к своим данным после миграции.

**Решение:**
- Создать скрипт миграции данных из localStorage в БД
- Добавить endpoint для импорта данных
- Предупредить пользователей о миграции

---

## 🟢 Мелкие проблемы и улучшения

### 6. Незавершенные TODO задачи

**Проблема:** В коде есть несколько TODO комментариев, указывающих на незавершенные задачи.

**Детали:**
- `frontend/components/BuyTokensSection.tsx` (строки 125, 559):
  - `// TODO: Add tokens to portfolio via API endpoint`
  - Токены не добавляются в портфель через API после покупки
  
- `frontend/app/api/stripe/webhook/route.ts` (строки 35, 51):
  - `// TODO: In production, update database here`
  - `// TODO: In production, handle failed payment`
  - Webhook не обновляет БД и не обрабатывает неудачные платежи

- `frontend/contexts/AuthContext.tsx` (строка 382):
  - `// TODO: Fetch actual TON balance from TON API if needed`
  - Баланс TON не загружается из API

**Решение:**
- Реализовать добавление токенов в портфель через API
- Обновить Stripe webhook для работы с БД
- Добавить загрузку баланса TON из API

---

### 7. Использование console.log вместо logger

**Проблема:** В некоторых местах все еще используется `console.log` вместо централизованного logger.

**Файлы:**
- `frontend/app/api/profile/route.ts` - использует `console.error`
- `frontend/app/api/profile/[address]/route.ts` - использует `console.error`
- `frontend/app/api/stats/route.ts` - использует `console.error`

**Решение:**
- Заменить все `console.*` на `logger.*`

---

### 8. Отсутствие индексов в БД

**Проблема:** Могут быть проблемы с производительностью при росте данных.

**Решение:**
- Добавить индексы для часто используемых полей
- Оптимизировать запросы

---

### 9. Отсутствие connection pooling

**Проблема:** Могут быть проблемы с производительностью при большом количестве запросов.

**Решение:**
- Настроить connection pooling для Prisma
- Оптимизировать запросы к БД

---

## 📋 Приоритетный план исправлений

### Фаза 1: Критические исправления (1-2 дня)
1. ✅ Завершить миграцию с localStorage на API
2. ✅ Добавить базовую аутентификацию
3. ✅ Добавить валидацию входных данных

### Фаза 2: Безопасность (1 день)
1. ✅ Добавить проверку владельца адреса
2. ✅ Добавить rate limiting
3. ✅ Заменить console.log на logger

### Фаза 3: Тестирование (1-2 дня)
1. ✅ Добавить unit-тесты для API
2. ✅ Добавить integration тесты
3. ✅ Создать скрипт миграции данных

### Фаза 4: Оптимизация (1 день)
1. ✅ Добавить индексы в БД
2. ✅ Настроить connection pooling
3. ✅ Оптимизировать запросы

---

## 📊 Статус компонентов

| Компонент | Статус | Проблемы |
|-----------|--------|----------|
| API Endpoints | ✅ Созданы | ❌ Нет защиты, нет валидации |
| useProfile Hook | ✅ Работает | ✅ OK |
| useProfileMutation | ✅ Работает | ✅ OK |
| profile-utils.ts | ⚠️ Устарел | ❌ Использует localStorage |
| DashboardSection | ⚠️ Частично | ❌ Использует localStorage для KYC banner |
| AuthContext | ⚠️ Частично | ❌ Возможно использует localStorage |
| Database | ✅ Настроена | ⚠️ Нет индексов |
| Logger | ✅ Реализован | ⚠️ Не везде используется |
| Error Handler | ✅ Реализован | ✅ OK |

---

## 🎯 Рекомендации

1. **Немедленно:** Завершить миграцию с localStorage на API
2. **Срочно:** Добавить базовую аутентификацию
3. **Важно:** Добавить валидацию входных данных
4. **Желательно:** Добавить тесты для API
5. **Опционально:** Оптимизировать БД

---

## 📝 Примечания

- Большинство проблем связаны с незавершенной миграцией на API
- Критичных багов, блокирующих работу, не обнаружено
- Проект находится в активной разработке
- Большинство функций работают, но требуют доработки

