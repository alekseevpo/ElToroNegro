# Как исправить существующую сеть Sepolia в MetaMask

## Проблема:
Ошибка: "Этот ID блокчейна в настоящее время используется сетью Sepolia"

Это означает, что сеть Sepolia уже добавлена, но нужно **редактировать** существующую, а не создавать новую.

## Решение 1: Редактировать существующую сеть (рекомендуется)

### Шаги:

1. **Закройте диалог "Добавить сеть"** (нажмите "Отмена" или крестик)

2. **Найдите существующую сеть Sepolia:**
   - В MetaMask нажмите на название текущей сети (вверху)
   - Прокрутите список сетей вниз
   - Найдите "Sepolia" или "Sepolia Test Network"

3. **Отредактируйте сеть:**
   - Нажмите на иконку настроек (⚙️) рядом с "Sepolia"
   - Или нажмите правой кнопкой мыши на "Sepolia" → "Edit" (Редактировать)
   - Или нажмите на "Sepolia" и найдите кнопку "Edit network" / "Изменить сеть"

4. **Измените RPC URL:**
   - В поле "URL-адрес RPC по умолчанию" замените старый URL на:
     ```
     https://ethereum-sepolia-rpc.publicnode.com
     ```
   - Или другой альтернативный:
     ```
     https://rpc.ankr.com/eth_sepolia
     ```

5. **Нажмите "Сохранить" (Save)**

## Решение 2: Удалить старую и создать новую

Если не можете найти кнопку редактирования:

1. **Удалите старую сеть Sepolia:**
   - Откройте список сетей в MetaMask
   - Найдите "Sepolia"
   - Нажмите на иконку корзины (🗑️) или "Remove" (Удалить)
   - Подтвердите удаление

2. **Создайте новую сеть:**
   - Нажмите "Add network" (Добавить сеть)
   - Нажмите "Add a network manually" (Добавить сеть вручную)
   - Введите следующие данные:

**Network Name:**
```
Sepolia
```

**RPC URL:**
```
https://ethereum-sepolia-rpc.publicnode.com
```

**Chain ID:**
```
11155111
```

**Currency Symbol:**
```
ETH
```

**Block Explorer URL:**
```
https://sepolia.etherscan.io
```

3. **Нажмите "Save" (Сохранить)**

## Решение 3: Использовать встроенную сеть Sepolia

MetaMask может иметь встроенную сеть Sepolia, которую нужно только включить:

1. Откройте список сетей в MetaMask
2. Нажмите "Show test networks" (Показать тестовые сети)
3. Найдите "Sepolia Test Network" в списке
4. Выберите её - она должна подключиться автоматически

## Рекомендация:

Сначала попробуйте **Решение 1** (редактирование существующей сети) - это проще и быстрее!

